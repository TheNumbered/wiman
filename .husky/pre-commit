# Run lint scripts in both frontend and backend
cd frontend && pnpm install && pnpm run lint
rm -rf node_modules
cd ../backend && pnpm install && pnpm run lint
rm -rf node_modules
cd ..
pnpm install && pnpm test
